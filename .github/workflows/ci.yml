name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Python toolchain (adjust if non-Python)
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          # install your package or deps if needed; safe no-op for now
          echo "No dependencies yet"

      - name: Lint
        run: |
          echo "lint placeholder"
          # e.g., ruff . || flake8 . ; keep as placeholder for now

      - name: Test
        run: |
          echo "test placeholder"
          # e.g., pytest -q

  # Optional: a very fast check you can require immediately
  quick:
    name: quick
    runs-on: ubuntu-latest
    steps:
      - run: echo "quick check"
